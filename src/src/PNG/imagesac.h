#ifndef _IMAGESAC_H_
#define _IMAGESAC_H_

#include "sac.h"

typedef char* string;

#define res_nt     (disp,  T_OLD((SCL, (HID, (NUQ,)))))
#define ar_nt      (ar,    T_OLD((AKD, (NHD, (NUQ,)))))
#define string_nt  (str,   T_OLD((SCL, (HID, (NUQ,)))))
#define filep_nt   (filep, T_OLD((SCL, (HID, (NUQ,)))))

#endif
